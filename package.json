{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "serve": "json-server --watch tmp/db.json",
    "start": "node api/server.js"
  }
}
